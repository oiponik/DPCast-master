import clientmanagerConfig from './clientmanager/ClientmanagerConfig';
import UpdatemanagerConfig from './updatemanager/UpdatemanagerConfig';
import MembermanagerConfig from './membermanager/MembermanagerConfig';

const appsConfigs = [
    clientmanagerConfig,
    UpdatemanagerConfig,
    MembermanagerConfig
];

export default appsConfigs;
